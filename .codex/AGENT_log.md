# AGENT Log

- [2026-02-10] Seed entry created because `.codex/AGENT_log.md` was missing. Keep this file process-focused (gotchas, workflows, conventions), not a changelog.
- [2026-02-11] Allowance display gotcha: some flows may surface approvals in normalized units; use `isUnlimitedAllowance` in widget UIs instead of one-off `maxUint` comparisons to avoid giant overflow numbers.
- [2026-02-11] Vitest default env is `node` in this repo (no `jsdom` package installed); prefer pure unit assertions for display-config tests unless jsdom is explicitly added.
- [2026-02-12] Use `bun run test <path>` (Vitest) rather than `bun test <path>` (Bun runner); the latter can report `document/window is not defined` for component tests.
- [2026-02-16] `bundlephobia` API may return HTTP 520 for many scoped packages from CLI checks; use npm registry metadata for maintenance gating and treat size checks as a manual follow-up when Bundlephobia is unavailable.
- [2026-02-17] In this WSL+sandbox setup, GUI open commands (`explorer.exe`, `xdg-open`) can fail; always provide the `.codex/diagrams/...` path explicitly so diagrams can be opened manually.
- [2026-02-26] OP staking holder scans can hit provider `eth_getLogs` caps on large ranges/results; prefer topic-filtered queries and adaptive chunking (retry with smaller ranges from provider error hints) before fallback tooling.
- [2026-02-26] For chained tx overlays where step 2 depends on step-1 state changes (e.g., unstake -> withdraw), expose a step-success callback from `TransactionOverlay` so parent state can switch steps before auto-continue readiness checks.
- [2026-02-26] Vault action arrays are not guaranteed to be length 2; mobile CTA rendering should derive primary/secondary actions by type, not positional indexes.
- [2026-02-26] For staked vault holdings/withdraw UX, normalize staking `balance.raw` with vault share decimals (not staking token metadata decimals) to avoid 18-vs-6 mispricing and tiny formatted amounts.
- [2026-02-26] Enso balances can include protocol position exposure for vault tokens; route Yearn vault/staking position tokens through multicall to avoid staking+vault double counting.
- [2026-02-26] Multi-step overlays should persist tx hash from `writeContractAsync` (not rely on `writeContract.data`) and refetch next-step simulations after step-1 receipts so unstake→withdraw doesn’t get stuck pending.
- [2026-02-26] Enso balance responses are sparse (usually non-zero holdings only); do not treat omitted tokens as “missing” for multicall fallback unless Enso request failed.
- [2026-02-26] Portfolio cumulative totals should be token-driven with staking-address precedence (staking mapping before direct vault mapping) to prevent double counting when a staking token is also listed as a vault token.
- [2026-02-27] Isolated helper/unit tests can hit a `tools.is -> constants -> tools.address` cycle when importing `toAddress`; prefer `viem/getAddress` for local address normalization in test-only routing helpers.
- [2026-02-27] In `TransactionOverlay`, avoid reset callbacks that depend on the full Wagmi `useWriteContract()` object; use a ref-backed `writeContract.reset` function to prevent close-state effect update loops.
- [2026-02-27] Staking integrations should use a shared source-aware ABI adapter (`VeYFI`/`yBOLD`/default) for stake, unstake, and withdraw-capacity reads; single-ABI unstake assumptions regress ERC4626 wrappers.
- [2026-02-28] Multi-step overlay auto-continue can double-submit step 2 if manual continue races scheduled auto-advance; gate advancement idempotently per executed step label and cancel pending auto when advancing.
- [2026-03-10] Synthetic vault rows like yvUSD are `TKongVaultView`s, not raw `TKongVault`s; any pinned/auxiliary list collections that can inject them should use `TKongVaultInput`.
- [2026-03-10] Synthetic vaults need full wallet integration, not just list injection: add their token addresses to `useYearnTokens`, handle their value fallback in `useWallet`, and special-case portfolio/list holdings paths if the main `vaults` map does not contain them.
- [2026-03-10] For synthetic portfolio rows, verify the APY side separately from the value side: a row can have correct aggregated USD value while still needing per-leg APY weighting for portfolio overview metrics.
- [2026-03-10] Offscreen measurement copies of the vault detail header must propagate `includeTourAttributes={false}` through vault-specific subcomponents like yvUSD holdings cards, or the welcome tour gets duplicate targets.
- [2026-03-10] Offscreen measurement/header clones can also expose SVG `<defs>` ID collisions that do not appear in single-render contexts; icons using `clipPath`/`mask`/gradients should prefer `useId()`-scoped IDs.
- [2026-03-10] Base widget compact-details mode should honor `hideDetails` and `hideActionButton`; synthetic wrappers like yvUSD can depend on those flags during pre-selection or cooldown states.
- [2026-03-10] When a synthetic wrapper intentionally keys a shared widget to reset child UI on mode switches, preserve typed input with a one-shot `prefill.amount` + `onPrefillApplied` handoff in the wrapper instead of removing the key.
- [2026-03-10] `InputTokenAmount` callbacks need the active token decimals passed through explicitly; otherwise wrapper state can silently treat 6-decimal display assets as 18-decimal raw amounts during shared widget flows.
- [2026-03-11] For wrapper-driven deposit flows that preselect a non-default input token (for example yvUSD locked USDC deposits), do not rely on wallet balances alone for token metadata; pass a stable token object into the shared widget so disconnected state still shows the correct symbol/logo.
- [2026-03-10] Locked yvUSD USDC deposits and withdrawals should use the dedicated locked-vault zap contract as a direct path; forcing either leg through the generic Enso route logic can leave the UI stuck on route errors or disabled actions.
- [2026-03-10] In parallel worker runs on a shared branch state, do not use broad `git restore --worktree` cleanup for non-owned files after repo-wide formatters; it can silently wipe other workers' accepted changes before they are reviewed or merged.
- [2026-03-11] Fresh git worktrees under `/tmp` do not inherit this repo's `node_modules`; for lint/build validation there, symlink the main repo `node_modules` into the worktree instead of assuming the build can resolve dependencies.
- [2026-03-11] In shared widget prefill flows, `onPrefillApplied` runs immediately after setting the input value and before wrapper `onAmountChange` callbacks observe the parsed amount; wrappers using one-shot override state should not clear that pending state in `onPrefillApplied`.
- [2026-03-12] For locked yvUSD cooldown withdraws, keep the contract-reported withdrawable share amount as the canonical cap and clamp any UI-derived reconversion back to that limit; round-tripping through displayed underlying amounts can overshoot `zapOut` with `ERC4626: redeem more than max`.
- [2026-03-17] For locked yvUSD withdraws, a successful `maxRedeem`/`previewZapOut` read of `0` is authoritative and must not fall back to `availableWithdrawLimit` or PPS math; treating zero as “missing” recreates a bogus 1-share withdraw dust path.
- [2026-03-12] In locked yvUSD withdraw flows, the wrapper’s pending requested amount can represent different raw units depending on mode (cooldown-start asset amount vs active-window share amount), so keep the state naming neutral and avoid “always shares” assumptions in refactors.
- [2026-03-12] If a branch that needs rebasing is checked out dirty in another worktree, do the rewrite on a throwaway branch/worktree and merge from there; avoid rewriting the checked-out ref until that worktree is clean.
- [2026-03-12] In this WSL+Codex setup, Playwright MCP `--extension` mode needs the Playwright MCP Bridge installed in the target Chrome profile; missing/wrong bridge shows either MCP `Extension connection timeout` or Chrome `ERR_BLOCKED_BY_CLIENT` on `chrome-extension://<extension-id>/connect.html`. Bridge tokens are profile-specific and can change after reinstall; after fixing the local MCP config, restart Codex to reload the browser tools. Prefer a stable local CLI path (or at least `npx -y`) over raw `npx @playwright/mcp@latest`.
- [2026-03-13] For vault-list performance work, compare warmed connected preview traces with `longTaskTotalMs` and long-task count before and after each fetch-staging change; extra backgrounding can regress real traces and should be reverted by measurement.
- [2026-03-16] Shared widget token overrides can target the same address as the base asset token; use `extraTokens` + `logoURI` when a flow needs alternate selector/input presentation without changing route logic or balances.
- [2026-03-16] `TokenSelector` can switch chains independently of the page context; when building chain-specific allowlists or metadata fallbacks there, use `useTokenList().tokenLists[selectedChainId]` instead of `currentNetworkTokenList`.
- [2026-03-16] In shared token selectors, keep `priorityTokens` for inclusion semantics and use a separate top-of-list ranking hint when deposit flows need common assets first without injecting zero-balance tokens into the default list.
- [2026-03-17] Detail-page bootstrap effects that both queue `requestAnimationFrame` work and flip local guard state can cancel their own fetch on rerender cleanup; trigger shared metadata fetches synchronously and use a ref for one-shot dedupe.
- [2026-03-17] `gh` auth can drift from working git credentials here; if `gh auth status` reports an invalid token in `~/.config/gh/hosts.yml` but `git push` still works, reseed CLI auth with `git credential fill` -> `gh auth login --with-token` instead of assuming GitHub access is fully broken.
- [2026-03-17] Tenderly public reads and admin cheats use different env vars here: `VITE_TENDERLY_RPC_URI_FOR_<chain>` drives the app transport, while `TENDERLY_ADMIN_RPC_URI_FOR_<chain>` drives `/api/tenderly/*` and CLI cheat scripts; after changing either, restart the dev/API process or stale ports can hide the fix.
- [2026-03-17] Locked yvUSD cooldown dust is partly contract-level: on exact-max cooldowns `maxRedeem` can be one share below `cooldownStatus.shares`, so UI-side “trim one share before cooldown” workarounds make residue worse by adding off-cooldown dust; after locked withdraw/cooldown txs, explicitly refetch `maxRedeem`/`maxWithdraw` alongside cooldown status to avoid stale max/dust UI.
- [2026-03-17] Locked yvUSD cooldown status copy should use the same `previewZapOut`-based amount source and higher-precision formatting as the input/max controls; falling back to two-decimal PPS formatting makes the “max” appear to change across cooldown stages even when the share cap did not.
- [2026-03-17] In locked yvUSD pre-cooldown flows, the requested amount is a locked-asset amount, not a share count; exact-max validation must compare against the derived cooldown asset cap, not `walletShares`, or `Max` will falsely trip `Insufficient balance` once PPS diverges from 1.
- [2026-03-17] In the no-zap locked yvUSD exit, the unlocked vault’s second leg can also diverge on `maxRedeem` vs `maxWithdraw` after step 1; use asset-based `withdraw(previewRedeem(shares))` for the unlocked leg instead of `redeem(shares)` or the flow can revert while a full asset withdraw is still valid.
- [2026-03-17] To isolate remaining yvUSD dust, compare post-flow `balanceOf`, `getCooldownStatus`, `maxWithdraw`, and `maxRedeem` on both locked and unlocked vaults; if unlocked is zero but locked balance remains above cooled shares, the residue is no longer a second-leg/zap issue.
- [2026-03-17] `tsconfig.json` includes repo-wide `**/*.ts`/`**/*.tsx`, so generated research bundles under `docs/temp` can break normal `tsc`/build if they contain copied source files; exclude those bundle directories explicitly or avoid `.ts/.tsx` extensions there.
- [2026-03-20] Vitest can also pick up copied `*.test.ts(x)` files under `docs/temp` during targeted runs, so research bundles should avoid test-file extensions or live under a Vitest exclude if you want `bun run test <path>` to stay truly narrow.
- [2026-03-19] For transient UI confirmations keyed off quote/route state, prefer keyed/resettable state (for example `{key, isAccepted}` with a render-time reset) over both `useEffect` resets and remembered-key booleans; remembered keys make `A -> B -> A` warnings sticky.
- [2026-03-19] On this Ubuntu 20.04 setup, Codex `0.116.0` broke local sandboxed commands (`bwrap --argv0` against system `bubblewrap 0.4.0`); rolling the Bun-global CLI back to `0.115.0` restored the sandbox after restarting Codex.
- [2026-03-19] gstack `browse` starts a localhost daemon for every command; in this Codex sandbox, blocked local socket binds surface as a misleading `No available port after 5 attempts...`, so rerun `browse` outside the sandbox before debugging gstack itself.
- [2026-03-19] The new gstack injected wallet can auto-connect the app and send raw `eth_sendTransaction` calls on Tenderly, but Yearn widget actions can still fail before submission if the page is hammering a rate-limited public Tenderly RPC; if the wallet is connected and a manual tx succeeds, point the app at the private/admin RPC before blaming the gstack wallet bridge.
- [2026-03-19] Do not hardcode the local app origin in repro docs or agents; probe `3000` and common preview ports first, and record the Tenderly fork block immediately after creating/switching VNets so the 50-block budget can be estimated before starting another full repro.
- [2026-03-20] Tenderly VNet creation accepts a named `rpc_config.rpc_name`; using a fixed profile-specific RPC name gives a stable public URL like `https://virtual.rpc.tenderly.co/<account>/<project>/public/<rpc_name>` so wallets do not need a new RPC every time, while the admin/private RPC still rotates per VNet instance.
- [2026-03-20] In chained no-zap yvUSD withdraws, freeze the first-leg locked-vault amount when the overlay opens, but let the second-leg unlocked-vault quote resolve against that frozen amount; freezing the step-2 quote too early can leave the overlay stuck after step 1 if `previewRedeem` had not populated yet.
- [2026-03-20] In workspace-write sessions, the `review-fix-loop` helper script needs `/tmp` for artifacts and its generated `codex exec review` invocation can mismatch the local CLI; if that happens, run `codex exec review --json --ephemeral -o /tmp/<file> --uncommitted` directly outside the sandbox instead.
- [2026-03-20] GitHub Advanced Security clear-text logging alerts on CLI helpers can flag benign env-derived labels; still audit adjacent stdout sinks like raw JSON/API responses or RPC URLs, because those may be the real secret exposure even when the inline alert points elsewhere.
- [2026-03-20] Kong Katana strategy composition rows can emit KAT incentives only in `performance.estimated.components.katRewardsAPR` while leaving `estimated.apr/apy` empty; when wiring strategy reward UI, read the component field before assuming the scalar APR exists.
- [2026-03-20] Locked yvUSD cooldown-window dust can hit an ERC4626 rounding split where `availableWithdrawLimit`/`maxWithdraw` stay at 1 raw asset unit while `maxRedeem` is 0; exact-max exits need a locked-leg `withdraw(maxWithdraw)` fallback for residual dust instead of pure redeem-only execution.
- [2026-03-20] The yvUSD withdraw wrapper shares `draftWithdrawAmount`/prefill state across locked and unlocked variants, so locked-only normalization effects must be gated by `isLockedVariant`; otherwise unlocked input gets snapped back to the locked max whenever a cooldown-window quote is still cached.
- [2026-03-20] `InputTokenAmount`’s balance prop affects both the displayed balance label and the 25/50/75/Max actions; wrappers like locked yvUSD that need “show total deposited, but cap actions to withdrawable/cooldown amount” must pass separate display and action balances instead of overloading one override.
- [2026-03-20] In the yvUSD withdraw wrapper, do not preserve/convert the amount when switching between locked and unlocked tabs; the two variants have different max semantics, so carrying shared input state across the toggle can silently strand an out-of-range amount without surfacing the right validation.
- [2026-03-23] In this sandbox, branch ref updates (`git switch`, `git cherry-pick`) can fail with a read-only `.git/index.lock`; rerun them with escalated permissions rather than assuming the repo is broken.
- [2026-03-23] When splitting a mixed feature stack, reconstruct the base infra branch first and then stack the widget/UI branch on top so the second PR stays reviewable even if the source history was interleaved.
- [2026-03-23] Non-interactive cherry-pick continues in this sandbox can drop into `nano`; set `GIT_EDITOR=true` on `git cherry-pick --continue` to avoid hanging on the commit message editor.
- [2026-03-24] Tenderly `evm_revert` consumes the target snapshot and invalidates later snapshots from that fork history; panel/state changes must either replace the spent snapshot immediately or mark local descendants unusable, or reset controls drift from actual chain state.
- [2026-03-24] Tenderly execution-chain remapping now layers on top of older localhost fork support; keep `1337` and `5402` as resolver-level passthrough execution IDs in runtime tests or local fork approvals/deposits/withdrawals can regress while Tenderly-specific flows still pass.
